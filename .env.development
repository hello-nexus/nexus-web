# Dev-mode override of the compiled-in production API origin (see
# src/api/nexusApi.ts). `vite build` ignores this file: builds keep the
# compiled-in production default unless VITE_API_URL is set explicitly.
VITE_API_URL=http://localhost:3000
