// Add-widget catalog preview fixture. See .agents/rules/widget-preview-fixtures.md:
// one complete, size-independent snapshot; SnakeWidget reads this instead of
// the real localStorage best score whenever PanelPreviewContext is active.
export const snakePreviewBest = 42;
