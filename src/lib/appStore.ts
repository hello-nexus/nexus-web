// Placeholder App Store URL used by the pairing redirect page and the
// insecure-panel banner. The iOS app distribution is still TestFlight /
// dev-signed (entitlement carries ?mode=developer), so this href is a
// stand-in until the real App Store listing exists. Update the literal
// here when the app is published; both call sites resolve via this
// constant.
export const APP_STORE_URL = 'https://apps.apple.com/app/qos/id0000000000';
